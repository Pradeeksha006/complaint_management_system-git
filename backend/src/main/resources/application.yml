server:
  port: 8082
  error:
    include-message: always
    include-binding-errors: always

spring:
  thymeleaf:
    check-template-location: false

  datasource:
    url: jdbc:mysql://localhost:3306/cms?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: ${DB_PASSWORD:pr@deeksha$#}

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:pradeeksha2006@gmail.com}
    password: ${MAIL_PASSWORD:ywggwtzisiatnwet}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          debug: true

  superadmin:
    address: ${ADMIN_EMAIL}
  admin:
    username: ${ADMIN_USERNAME}
    email: ${ADMIN_EMAIL}
    password: ${ADMIN_PASSWORD}
  multipart:
    max-file-size: 50MB
    max-request-size: 50MB

# Custom Application Configs
app:
  jwt:
    secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
    accessTokenExpirationMs: 86400000
    refreshTokenExpirationMs: 604800000

  admin:
    username: ${ADMIN_USERNAME:superadmin}
    email: ${ADMIN_EMAIL:pradeeksha2006@gmail.com}
    password: ${ADMIN_PASSWORD:Pradhi@1234}

  cloudinary:
    cloud-name: ${CLOUDINARY_CLOUD_NAME:placeholder}
    api-key: ${CLOUDINARY_API_KEY:placeholder}
    api-secret: ${CLOUDINARY_API_SECRET:placeholder}

  frontend:
    url: ${FRONTEND_URL:https://frontend-nu-peach-88.vercel.app}

  gemini:
    api-key: ${GEMINI_API_KEY:}
    model: ${GEMINI_MODEL:gemini-2.0-flash}
